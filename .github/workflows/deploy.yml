name: Deploy

on:
  push:
    branches: [claude/world-cup-sweep-app-oFZHS]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install app dependencies
        run: npm ci

      - name: Deploy Cloudflare Worker
        working-directory: worker
        run: npx wrangler deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        continue-on-error: true

      - name: Set Worker YouTube API key
        working-directory: worker
        run: echo "$YOUTUBE_API_KEY" | npx wrangler secret put YOUTUBE_API_KEY
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          YOUTUBE_API_KEY: ${{ secrets.YOUTUBE_API_KEY }}
        continue-on-error: true

      - name: Set Worker YouTube API key backup
        working-directory: worker
        run: echo "$YOUTUBE_API_KEY_BACKUP" | npx wrangler secret put YOUTUBE_API_KEY_BACKUP
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          YOUTUBE_API_KEY_BACKUP: ${{ secrets.YOUTUBE_API_KEY_BACKUP }}
        continue-on-error: true

      - name: Build React app
        run: npm run build
        env:
          VITE_WALL_API_URL: https://worldcup.phil-remington.workers.dev
          VITE_YOUTUBE_API_KEY: ${{ secrets.YOUTUBE_API_KEY }}
          VITE_YOUTUBE_API_KEY_BACKUP: ${{ secrets.YOUTUBE_API_KEY_BACKUP }}

      - name: Install England Campaign dependencies
        working-directory: premier
        run: npm ci

      - name: Check the fixture parser against the live feed
        working-directory: premier
        run: node scripts/check-fixtures.mjs
        continue-on-error: true

      - name: Unit tests
        working-directory: premier
        run: |
          node scripts/leaguefeed.test.mjs
          node scripts/formation.test.mjs
          node scripts/highlight-title.test.mjs
          node scripts/matchdetail.test.mjs

      - name: Check live-score feed (diagnostic only, never blocks)
        working-directory: premier
        run: node scripts/check-feeds.mjs
        continue-on-error: true

      - name: Fetch club crests (falls back to generated insignia if unavailable)
        working-directory: premier
        run: npm run crests
        continue-on-error: true

      - name: Build England Campaign app
        working-directory: premier
        run: npm run build
        env:
          VITE_WALL_API_URL: https://worldcup.phil-remington.workers.dev

      - name: Add England Campaign at /england
        run: cp -r premier/dist dist/england

      # Stamp each build so gh-pages always gets a new commit — without this,
      # a retriggered deploy with identical output is silently skipped and
      # GitHub Pages never re-publishes after a transient failure
      - name: Stamp build
        run: echo "${{ github.sha }} $(date -u +%Y-%m-%dT%H:%M:%SZ)" > dist/build-stamp.txt

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          publish_branch: gh-pages
          force_orphan: false
